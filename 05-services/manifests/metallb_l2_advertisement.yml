# Announce IPs from the pool using L2 ARPs. Alternatively, BGP is also supported.
apiVersion: metallb.io/v1beta1
kind: L2Advertisement 
metadata:
  name: metallb-pool
  namespace: metallb-system
spec:
  ipAddressPools:
  - metallb-pool