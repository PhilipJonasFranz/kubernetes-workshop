apiVersion: v1
kind: Service
metadata:
  name: web-stateful # Name must match the service name we specified in the statefulset manifest
  namespace: default
  labels:
    app: web-stateful
spec:
  clusterIP: None # Marks this as a headless service
  selector:
    app: web-stateful
  ports:
    - port: 80